spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    url: jdbc:mysql://doc.fzuhuahuo.cn:3307/sosd_cloud_database?serverTimezone=UTC
    username: root
    password: huahuolalala
  redis:
    # Redis数据库索引（默认为0）
    host: 43.139.142.207
    # 端口，默认为6379
    port: 6379
    # 数据库索引
    database: 5
    # 密码(如没有密码请注释掉)
    password: 20040816Ywj
    username: default
    # 连接超时时间
    timeout: 10s
    lettuce:
      pool:
        # 连接池最大连接数
        max-active: 200
        # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1ms
        # 连接池中的最大空闲连接
        max-idle: 10
        # 连接池中的最小空闲连接
        min-idle: 0
